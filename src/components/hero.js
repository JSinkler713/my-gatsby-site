import React from 'react';
import styled from '@emotion/styled';
import { Link, graphql, useStaticQuery } from 'gatsby';

const Hero = () => {
  return(
    <div>
      <h1>J Sinkler Dev</h1>
      <p>Creating sick sites</p>
    </div>
  )
}

export default Hero;
